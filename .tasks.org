* Org mcp
  :PROPERTIES:
  :ID: 48524342
  :END:
  The structural rendering fix is now fully operational. This text is added via the unified toolset to verify that plain text is correctly prioritized over sub-headers.
** Headers
   :PROPERTIES:
   :ID: 28028254
   :END:
*** Modification
    :PROPERTIES:
    :ID: 92335292
    :END:
**** DONE change status by index [6/6]
     CLOSED: [2025-11-01 Sat 20:46]
     :PROPERTIES:
     :ID: 99590227
     :END:
     - [x] implement the modification as a method on the OrgFile type
     - [x] allow the tool call to generate the OrgFile from the input file
     - [x] return the modified headers as a list of rendered entries
     - [x] verify every status for this behaviour
     - [x] deduplicate headers in the response
     - [x] allow multiple indeces at once
*** Metadata
    :PROPERTIES:
    :ID: 32102203
    :END:
    - [x] parse the closed schedule tag
    - [x] parse the scheduled schedule tag
    - [x] parse the deadline schedule tag
    - [x] parse the date as an actual datetime
**** DONE parse properties
     :PROPERTIES:
     :ID: 82181185
     :END:
**** DONE automatically set CLOSED tag
     CLOSED: [2026-02-16 Mon 17:03]
     :PROPERTIES:
     :ID: 14898686
     :END:
     - [x] Analyze the current structure and data model to identify where the CLOSED field fits.
       * The CLOSED tag in org mode is used to record the timestamp when a task is marked as DONE.
         In the org-mcp project, status transitions (such as from TODO/PROG to DONE)
         should trigger logic that sets the CLOSED property to the current timestamp.
         The data model already parses CLOSED tags,
         so the implementation should focus on updating the header's properties during status changes
         and ensuring this is reflected in both the in-memory structure and the persisted org file.
       - [x] Identify where status transitions occur in the code (e.g., from TODO/PROG to DONE).
       - [x] Implement logic during status transitions to set CLOSED to the current timestamp.
       - [x] Write unit tests to validate that the CLOSED tag logic works properly.
       - [x] Document implementation details and edge cases in project documentation.
**** TODO modification of properties
     :PROPERTIES:
     :ID: 55803461
     :END:
     - [ ] insert properties to the map
     - [ ] delete properties from the map
     - [x] modify properties in the map
     Test text for manage_text
     Test text for manage_text
     Another test text
**** TODO modification of schedule tags
     :PROPERTIES:
     :ID: 88799343
     :END:
     - [ ] allow a schedule to be added to headers
     - [ ] allow modifying schedule date
     - [ ] CLOSED should always be automatic
**** TODO automatically set SCHEDULED tag
     :PROPERTIES:
     :ID: 24597521
     :END:
     - [ ] Create a function to automatically set the scheduled tag to today within the Golang code.
     - [ ] Write scheduled function implementation, capable of fetching current date and assigning it to schedule tag.
     - [ ] Ensure tests are written to validate scheduled tag is correctly set to today's date in different header states.
     - [ ] Convert the header status change logic to include scheduled tags by default.
     - [ ] Define test cases to validate that scheduled tags are set correctly for valid state transitions involving 'NEXT'.
     - [ ] Test the scenario where headers already contain a scheduled tag and ensure proper behavior when transitioning to NEXT.
*** Parsing
    :PROPERTIES:
    :ID: 87835160
    :END:
**** TODO add a constructor based solely from a PeekReader instance
     :PROPERTIES:
     :ID: 46024702
     :END:
*** Searching
    :PROPERTIES:
    :ID: 8406955
    :END:
**** DONE Vector search
     :PROPERTIES:
     :ID: 04236728
     :END:
*** DONE Optimize Embedding Storage with JSON Cache
    CLOSED: [2026-03-22 Sun 17:24]
    :PROPERTIES:
    :ID: 37383517
    :END:
    - [ ] Analyze potential for a performance bottleneck in embedding generation for each vector search request.
**** DONE Resolved: Persistence and Regeneration Bottleneck
     CLOSED: [2026-03-22 Sun 17:24]
     :PROPERTIES:
     :ID: 23501976
     :END:
     - [x] Vector search currently regenerates embeddings for every search request
     - [x] Creates significant performance bottleneck, especially for large org files
     - [x] Wastes API calls to embedding providers (OpenAI, etc.) or CPU cycles for local models
     - [x] No persistence - embeddings lost on server restart
     - [x] Regeneration time increases linearly with file size
**** DONE Advantages of JSON-based Storage
     CLOSED: [2026-03-22 Sun 17:24]
     :PROPERTIES:
     :ID: 97146068
     :END:
     - [ ] Dramatically reduces search latency by caching embeddings instead of regenerating
     - [ ] Cost efficiency - reduces expensive API calls to embedding providers
     - [ ] Performance: Fast reading/writing for medium-sized task files
     - [ ] Persistence - embeddings survive server restarts
     - [ ] Simplicity: Easy implementation with minimal dependencies
     - [ ] Incremental updates - only regenerate embeddings for modified headers
     - [ ] Portability: Easy to inspect, backup, and move alongside the .tasks.org file
**** TODO Implementation Strategy
     :PROPERTIES:
     :ID: 39886024
     :END:
***** Database Schema
      :PROPERTIES:
      :ID: 30138340
      :END:
      - [ ] Design embeddings table: (header_uid PRIMARY KEY, content_hash TEXT, embedding_blob BLOB, model_name TEXT, dimensions INTEGER, created_at TIMESTAMP, updated_at TIMESTAMP)
      - [ ] Add index on content_hash for fast lookups
      - [ ] Consider storing additional metadata (org file path, header title) for debugging
***** Cache Invalidation
      :PROPERTIES:
      :ID: 94949826
      :END:
      - [ ] Store SHA-256 hash of header content alongside embeddings
      - [ ] Compare content hash on each search to detect changes
      - [ ] Only regenerate embeddings for headers with changed content hash
      - [ ] Implement orphan cleanup to remove embeddings for deleted headers
***** Performance Optimization
      :PROPERTIES:
      :ID: 98802810
      :END:
      - [ ] Evaluate sqlite-vss extension for native vector similarity search
      - [ ] Evaluate sqlite-vec extension as alternative
      - [ ] Consider background worker for batch regeneration of stale embeddings
      - [ ] Implement connection pooling for concurrent access
***** Migration Path
      :PROPERTIES:
      :ID: 62771632
      :END:
      - [ ] Design backward-compatible initialization (create DB if not exists)
      - [ ] Add CLI flag to force regeneration of all embeddings
      - [ ] Support graceful fallback if database is corrupted
**** TODO Key Considerations
     :PROPERTIES:
     :ID: 47380298
     :END:
     - [ ] File size: Embeddings are large (~6KB per header for 1536 dimensions) but SQLite handles blobs well
     - [ ] Concurrency: SQLite's read concurrency is excellent; write locks shouldn't be an issue for read-heavy workload
     - [ ] Storage location: Store DB alongside org file or in XDG cache directory?
     - [ ] Schema versioning: Plan for future schema changes with version table
     - [ ] Orphan cleanup: Need strategy to detect and remove embeddings for deleted headers
     - [ ] Multiple org files: How to handle embeddings for multiple org files (one DB per file or unified DB)?
     - [ ] Error handling: What happens if embedding generation fails for some headers?
**** TODO Implementation Tasks
     :PROPERTIES:
     :ID: 55845642
     :END:
     - [ ] Research and choose SQLite library (modernc.org/sqlite vs mattn/go-sqlite3)
     - [ ] Research and evaluate vector extensions (sqlite-vss vs sqlite-vec)
     - [ ] Design database schema with proper indexes
     - [ ] Implement database initialization and migration logic
     - [x] Implement JSON-based cache storage layer
     - [x] Implement content hashing for cache invalidation
     - [x] Add logic to detect stale embeddings based on content changes
     - [ ] Add CLI flags for cache management (--clear-cache, --rebuild-cache, --cache-stats)
     - [ ] Write unit tests for storage layer (CRUD operations, concurrent access)
     - [ ] Write unit tests for cache hit/miss scenarios
     - [ ] Write integration tests for cache invalidation on content changes
     - [ ] Benchmark performance: compare cached vs non-cached search times
     - [ ] Document the caching strategy in README or docs/
     - [ ] Document maintenance procedures (cache clearing, troubleshooting)
** Bullets
   :PROPERTIES:
   :ID: 51396790
   :END:
*** DONE Bullet parsing
    :PROPERTIES:
    :ID: 83577245
    :END:
*** DONE Bullet modification [4/4]
    CLOSED: [2025-11-09 Sun 11:26]
    :PROPERTIES:
    :ID: 89764195
    :END:
**** DONE bullet creation
     CLOSED: [2025-11-08 Sat 17:06]
     :PROPERTIES:
     :ID: 82673695
     :END:
     - [x] Add support for omitting the index when creating a bullet. if no index is specified, append the bullet to the end rather than inserting at a specific position.
**** DONE bullet completion [9/9]
     :PROPERTIES:
     :ID: 62550511
     :END:
     - [x] Review both the bullet completion and manage bullet tools in main.go and document differences and overlap.
     - [x] Analyze interfaces and implementation of complete_checkbox and manage_bullet_point in main.go for overlap and differences.
     - [x] Design a unified tool input schema that supports bullet add, remove, and completion (toggle/complete) operations via a 'method' argument.
     - [x] Design a unified tool implementation, merging manage_bullet_point and complete_checkbox logic into a single handler with branching on method.
     - [x] Update error handling and shared routines to prevent code duplication and improve API consistency.
     - [x] Update and combine the tool documentation (InputSchema, description) to cover all operations.
     - [x] Update/extend all relevant unit tests, covering add, remove, and complete logic through the unified tool.
     - [x] Test the MCP integration to ensure the unified tool performs correctly end-to-end.
     - [x] Create a short report after implementation summarizing combined logic, new tool schema, example usage, and migration steps if needed.
**** DONE bullet deletion [4/4]
     DEADLINE: <2025-11-08 Sat>
     :PROPERTIES:
     :ID: 47234894
     :END:
     - [x] Implement bullet deletion function.
     - [x] Integrate bullet deletion in the bullet createion mcp tool.
     - [x] Write unit tests for bullet deletion.
     - [x] Test the MCP tool for correctness of execution and return values.
**** DONE synchornise header progress
     CLOSED: [2025-11-09 Sun 11:26]
     :PROPERTIES:
     :ID: 88266361
     :END:
     - [x] Parially done already with the =CheckProgress= function
       * Analyzed existing CheckProgress and header status logic.
         Confirmed CheckProgress sets header to DONE when progress is complete.
         Will update logic to set status to PROG if any checkbox is checked,
         and further synchronize with checkbox state.
         Planning to ensure thorough unit testing for any changes.
       - [x] Change header status automatically from =NEXT=/=TODO= to =PROG= if a single checkbox is set
       - [x] Change header status to =DONE= if =CheckProgress= has =Done()= as true.
       - [x] Unit test new functionality
*** PROG Bullet children [4/7]
    SCHEDULED: <2026-01-26 Mon> DEADLINE: <2026-02-02 Mon>
    :PROPERTIES:
    :ID: 90135268
    :END:
    - [x] Add a children field to the bullet struct
    - [ ] Assign a progress to a bullet point
    - [ ] Allow partially completed bullets =[-]=
    - [x] Parse bullet children from the reader by peeking
    - [ ] Bullet points can have progress counters =[0/2]=
    - [x] Children are always indented by 2 spaces
    - [x] IDs should still be calculated by the index inside the header
** Plain content
   :PROPERTIES:
   :ID: 18319364
   :END:
*** DONE Plain text parsing [6/6]
    SCHEDULED: <2025-11-08 Sat> CLOSED: [2025-11-11 Tue 22:00]
    :PROPERTIES:
    :ID: 97732298
    :END:
    - [x] Extend the =ParseIndentedLine= function so it has a fallback to anything other than bullets etc.
    - [x] Add a =Text= type that implements =Render=
    - [x] Assign the default path of the switch case in =ParseIndentedLine= to a =Text= renderable.
    - [x] Unit test =Text= parsing
    - [x] Unit test rendering of =Text=
    - [x] Test the MCP integration by fetching a header and checking whether the plain text is returned as well.
*** DONE Plain text modification
    SCHEDULED: <2026-01-31 Sat> DEADLINE: <2026-02-02 Mon> CLOSED: [2026-02-12 Thu 20:43]
    :PROPERTIES:
    :ID: 73284623
    :END:
**** DONE add method
     CLOSED: [2026-02-12 Thu 20:43]
     :PROPERTIES:
     :ID: 46348459
     :END:
**** DONE modify method
     CLOSED: [2026-02-12 Thu 20:43]
     :PROPERTIES:
     :ID: 71553477
     :END:
**** DONE remove method
     CLOSED: [2026-02-12 Thu 20:43]
     :PROPERTIES:
     :ID: 71251836
     :END:
**** DONE testing
     CLOSED: [2026-02-12 Thu 20:43]
     :PROPERTIES:
     :ID: 13815625
     :END:
*** TODO Paragraph consolidation (Block Parsing)
    :PROPERTIES:
    :ID: 18065620
    :END:
    - [ ] Modify ~NewPlainTextFromReader~ in ~orgmcp/plain.go~ to peek at subsequent lines.
    - [ ] Logic: Continue consuming if the next line has the same indentation and no ~*~ or ~-~ prefix.
    - [ ] Update ~PlainText.Render~ to ensure internal newlines are preserved correctly.
    - [ ] Ensure ~PlainText.Uid~ logic remains stable with multi-line blocks.
    - [ ] Add unit tests in ~test/files/plain_text.org~ for multi-line paragraph verification.
    Tests implemented in tools/test/text_tool_test.go covering add, update, remove, ShowAffected, ShowDiff, newline handling, and error cases
** Dates and schedules
   :PROPERTIES:
   :ID: 45374501
   :END:
*** PROG Date Parsing :date:parsing:
    :PROPERTIES:
    :ID: 78268550
    :END:
    - [x] Analyze the org mode syntax for various date formats and ensure compliance with parsing standards.
    - [x] Develop a parser to read CLOSED and SCHEDULED timestamps, ensuring timezone support.
    - [ ] Write tests for parsing various edge cases, such as invalid or incomplete dates.
*** DONE Date Searching
    CLOSED: [2026-02-02 Mon 18:52]
    :PROPERTIES:
    :ID: 94835494
    :END:
    - [x] Implement exact match searching for SCHEDULED and DEADLINE tags.
    - [x] Implement range searching (Between A and B) for all timestamp types.
    - [x] Implement 'Overdue' logic: DEADLINE < Now AND Status != DONE.
    - [x] Add 'Completed' history search using the CLOSED timestamp.
    - [x] Support 'Missing' search to find tasks without assigned dates.
    - [x] Implement `DateFilter` struct inside `ViewItem` for localized date-based queries.
    - [x] Handle negative `Range` logic: Anchor date becomes the exclusive end-cap for past searches.
    - [x] Ensure date normalization (midnight-to-midnight) to prevent time-of-day filtering bugs.
    - [x] Add `ShowClosed` toggle to filter between active agenda and completion history.
*** TODO Schedule Modification
    :PROPERTIES:
    :ID: 22890236
    :END:
    - [ ] Support adding new schedules while validating for allowed date ranges.
    - [ ] Implement endpoint to modify dates (schedule adjustments) dynamically
*** TODO Allow reoccurring tasks
    :PROPERTIES:
    :ID: 15953659
    :END:
** Markdown rendering
   :PROPERTIES:
   :ID: 48165813
   :END:
   - [x] Implement colored statuses in Markdown export using LaTeX syntax (e.g., $\color{red}{\textsf{TODO}}$) controlled by the ~MARKDOWN_COLOR~ environment flag.
   - [x] Implement regex-based conversion for Org font styles (*bold*, //italics//, ~code~) to Markdown (**bold**, *italics*, `code`) in the renderer.
** AI Session Context & Tooling Progress :context:ai:session:
   :PROPERTIES:
   :ID: 96275729
   :END:
   * Optimized ~query_items~ tool with CSV output, deduplication, and document-order sorting.
   * Implemented ~PATH~ column using a UID-stack format (e.g., ~//ROOT/PARENT/ID~) for token-efficient navigation.
   * Added ~PREVIEW~ and ~TAGS~ (comma-quoted) columns to separate core text from Org-syntax noise.
   * Confirmed regex filtering on ~CONTENT~ matches against the ~PREVIEW~ text.
   * Verified that ~manage_header~ handles sequential updates and queries in a single multi-item request.
   * Verified that ~show_diff: true~ provides high-fidelity feedback for structural changes, reducing the need for follow-up ~query_items~ calls.
   * Confirmed that bullet and text UIDs are unstable (index-based) and must be re-queried after any structural modification.
*** Data Formats & CSV Optimization
    :PROPERTIES:
    :ID: 32788174
    :END:
    * Primary tool output is standardized to CSV for maximum token efficiency.
    * Fields are automatically quoted (via ~encoding/csv~) to safely handle internal commas in content or tags.
    * ~PREVIEW~ column provides the 'clean' title/text, while ~CONTENT~ provides the raw Org-syntax for editing.
*** Query & Search Logic
    :PROPERTIES:
    :ID: 45715638
    :END:
    * Multi-item requests in ~manage_header~ and ~query_items~ are processed sequentially, allowing a single call to 'Update' then 'Get' a header state.
    * Global deduplication logic ensures unique UIDs in a single unified CSV response, even with overlapping depth/filter parameters.
    * Results are sorted by their original position in the document to preserve semantic flow.
*** Navigation & Hierarchical Context
    :PROPERTIES:
    :ID: 30640212
    :END:
    * The ~PATH~ column provides a token-efficient 'GPS' using UIDs (e.g., ~//ROOT/PARENT/ID~) to map hierarchy without repeating long titles.
    * Metadata columns like ~PARENT~ and ~CHILDREN_COUNT~ allow the model to navigate the tree structure without deep-reading content.
*** DONE Implemented CSV Columns [12/12]
    :PROPERTIES:
    :ID: 42321214
    :END:
    - [x] ~TYPE~: Go type of the renderable item (e.g., ~*orgmcp.Header~, ~*orgmcp.Bullet~).
    - [x] ~UID~: Unique identifier (string or integer) for the item.
    - [x] ~PREVIEW~: Clean text content without Org-mode syntax/markers. Best for token-efficient browsing.
    - [x] ~CONTENT~: Raw Org-mode content, including syntax and newlines (escaped as ~\n~).
    - [x] ~STATUS~: Header status (TODO, NEXT, PROG, DONE, etc.). Empty for bullets/text.
    - [x] ~PROGRESS~: Progress ratio (e.g., ~2/3~) for items with children/checkboxes.
    - [x] ~PARENT~: UID of the immediate parent item.
    - [x] ~CHILDREN_COUNT~: Number of direct children under the item.
    - [x] ~TAGS~: Comma-separated list of tags, quoted for CSV safety (e.g., ~"tag1,tag2"~).
    - [x] ~PATH~: Hierarchical stack of UIDs from root to item (e.g., ~//UID1/UID2/ID~).
    - [x] ~LEVEL~: Numeric depth of the item (e.g., 1, 2, 3) to allow tree reconstruction without parsing stars.
    - [x] ~SCHEDULED~ / ~DEADLINE~ / ~CLOSED~: Dedicated date columns for high-performance scheduling queries.
*** Design Philosophy & Future Ideas
    :PROPERTIES:
    :ID: 74856328
    :END:
    * *AI-Native Goal*: Prioritize 'steering' data (UIDs, status, progress) over raw 'text' data to keep context windows small.
    * *CSV vs JSON*: CSV is preferred for bulk queries due to its higher data density and lower token overhead (no repeating keys).
    * *Document Order*: Always preserve file order in query results, as sequence is semantic in Org-mode. Avoid sorting by UIDs unless explicitly requested.
    * *Potential Column: STATUS_COUNTS*: Consider adding an aggregated view of children's statuses (e.g., TODO:3, DONE:5) to headers.
    * *Potential Column: LEVEL*: Adding numeric depth would allow models to reconstruct the tree structure without parsing Org stars.
    * *Dedicated Scheduling Columns*: Add ~DEADLINE~, ~SCHEDULED~, and ~CLOSED~ as dedicated CSV columns for high-performance date-based querying.
    * *Priority Support*: Support Org-mode priorities (e.g., ~[#A]~, ~[#B]~) as a standalone column.
    * *Body Preview Control*: Allow the ~PREVIEW~ or a new ~SUMMARY~ column to return a specific number of lines from the body text.
    * *Property Export*: A ~PROPERTIES~ column that returns all property drawer key-values as a parseable string.
    * *Date Range Filtering*: Add the ability to filter results by date ranges (e.g., ~SCHEDULED~ or ~DEADLINE~ between Date A and Date B) directly in ~query_items~.
    * *Bulk Operation Efficiency*: Using multi-item tool calls (e.g., adding 4 sub-headers in one ~manage_header~ call) is significantly more token-efficient than sequential calls.
    * *Surgical Editing Pattern*: Prefer ~manage_bullet~ and ~manage_header~ over ~edit_file~ to preserve property drawers and structural metadata automatically.
    - [ ] *Persistent Memory*: Use the org file itself as persistent memory for AI sessions. Track session context, files being worked on, known bugs, and 'known unknowns' (edge cases that don't work). This addresses AI limitations around persistent mental workspace and goal tracking across interactions.
    - [ ] *Bug Pattern: CLOSED schedule parsing*: When parsing CLOSED timestamps in `schedule.go`, the regex `.*CLOSED: \[(.*? )(.*? )?(.*?)?\].*` had a trailing space in the first capture group. This caused `time.Parse("2006-01-02", matches[1])` to fail on dates without times. Fixed by changing to `(.*?)` to exclude the trailing space.
    * *Table Support*: Implement parsing and surgical modification of Org-mode tables with CSV-style row/column targeting.
    * *Advanced Date Tools*: Add dedicated tools for scheduling, deadlines, and reoccurring tasks with relative date support (+2d, next week).
*** TODO Task History Tracking in Properties :design:
    :PROPERTIES:
    :ID: 21285539
    :END:
    - [ ] Problem: AI models lose context when tasks span multiple sessions. When tunnel vision sets in, the AI forgets what was attempted before.
    - [ ] Solution: Store task history in the :PROPERTIES: drawer of each header. Properties are collapsed by default so they don't clutter the visual org file view.
    - [ ] Property format: :HISTORY: [{"action": "create", "field": "status", "old": null, "new": "TODO", "timestamp": "..."}, ...]
    - [ ] Actions to track: CREATE, UPDATE (status, content, tags), DELETE, MOVE
    - [ ] New tool: history_tool that returns CSV with columns: UID, ACTION, FIELD, OLD_VALUE, NEW_VALUE, TIMESTAMP
    - [ ] Tool filters: by UID, by date range, by action type
    - [ ] Implementation: When any manage_* tool modifies a header, append to the :HISTORY: property before writing to disk
** DONE Derive Tool Input Schema from Go Struct using Reflection :jsonrpc:schema:input:
   CLOSED: [2026-02-16 Mon 15:54]
   :PROPERTIES:
   :ID: 15951102
   :END:
   - [x] Use the `reflect` package to traverse Go struct fields and tags.
   - [x] Map Go types (string, int, bool, slices, etc.) to JSON Schema types.
   - [x] Extract descriptions, required status, and constraints from struct tags (e.g., `jsonschema:"description=..."`).
   - [x] Ensure nested structs are handled recursively to build complex input schemas.
   - [x] Support `json:",omitempty"` to distinguish between required and optional fields.
   - [x] Ensure `default` and `enum` values in the schema match the field's JSON type (e.g., numbers instead of strings for int fields).
   - [x] Implement a registration helper in `mcp.Server` to simplify tool definition.
   - [x] Support `oneOf` in the schema definition for fields that can have multiple valid schemas (e.g., specific constants with titles).
   - [x] Support `anyOf` in the schema definition for fields that can have multiple valid schemas selected (e.g., multiple flags or options).
   - [x] Support `minLength` and `maxLength` for string length constraints.
   - [x] Support `pattern` for regex validation on strings.
   - [x] Support `format` for string formats (email, uri, date-time, etc.).
   - [x] Support `minimum` and `maximum` for numeric bounds.
** NEXT Movement and Reordering :move:order:
   SCHEDULED: <2026-02-14 Sat> DEADLINE: <2026-02-15 Sun>
   :PROPERTIES:
   :ID: 65811682
   :END:
   * *Problem*: Currently moving requires a ~remove~ and then an ~add~, which is inefficient and loses state if not done carefully.
   * *Constraint*: Moving an item changes its index, which in turn changes the UIDs for Bullets and PlainText (e.g., ~.b0~ becomes ~.b1~).
   * *Idea*: Implement a ~move~ tool that takes a ~source_uid~, ~target_parent_uid~, and ~target_index~.
   * *Implementation*: Need a ~MoveChild(uid Uid, targetParent Uid, index int)~ method in ~OrgFile~.
*** TODO Phase 1: Internal Logic (Foundation)
    :PROPERTIES:
    :ID: 41461318
    :END:
    - [ ] Extend the ~Render~ interface in ~orgmcp/render.go~ with ~MoveChild(source Uid, targetIndex int)~.
    - [ ] Implement ~MoveChild~ in ~Header~, ~Bullet~, and ~OrgFile~ to support reordering within the same parent.
    - [ ] Implement a global ~Move(source Uid, targetParent Uid, targetIndex int)~ in ~OrgFile~ to handle cross-parent movement.
    - [ ] Ensure ~Header.SetLevel~ is called recursively when a header is reparented to a new depth.
*** TODO Phase 2: MCP Tool Implementation
    :PROPERTIES:
    :ID: 62507500
    :END:
    - [ ] Create a new ~move_item~ tool in a new file ~tools/move_tool.go~.
    - [ ] Define the input schema: ~source_uid~ (string, required), ~target_parent_uid~ (string, required), ~target_index~ (int, optional).
    - [ ] Support semantic targets like ~"top"~ or ~"bottom"~ for the ~target_index~ to make it easier for LLMs.
    - [ ] Support *Relative Mode*: Add ~before_uid~ and ~after_uid~ as optional alternatives to ~target_index~.
    - [ ] Support *Swap Mode*: Implement a specific method to atomically exchange the positions of two UIDs.
*** TODO Phase 3: Validation & Tests
    :PROPERTIES:
    :ID: 41514169
    :END:
    - [ ] Unit test: Reordering bullets within a single header and verifying UID updates (e.g., .b0 -> .b1).
    - [ ] Unit test: Moving a header between two different parent headers and verifying structural integrity.
    - [ ] Integration test: Using the MCP tool to move a bullet and checking the diff output.
** TODO Exporting & Integration (Notion) :export:notion:
   :PROPERTIES:
   :ID: 11607416
   :END:
   * *Goal*: Enable exporting Org-mode content to Notion for work collaboration.
   * *Current State*: Purely CRUD on local .org files.
   * *Dependency*: Requires Notion API access and mapping logic from Org hierarchy to Notion blocks.
*** TODO Phase 1: Foundation & Notion API Integration
    :PROPERTIES:
    :ID: 3680641
    :END:
    - [ ] Research Notion API go client libraries (or use raw HTTP).
    - [ ] Implement authentication handling (Environment variables or MCP configuration).
    - [ ] Define a ~NotionExporter~ struct to manage the connection and state.
*** TODO Phase 2: Core Export Logic & Mapping
    :PROPERTIES:
    :ID: 7874898
    :END:
    - [ ] Implement mapping from Org Level to Notion Block Type (e.g., Level 1 -> Header 1, Level 2 -> Header 2).
    - [ ] Map Org Bullets to Notion ~bulleted_list_item~ blocks.
    - [ ] Support basic rich text conversion (Bold, Italics, Monospace).
*** TODO Phase 3: Advanced Mapping & Database Support
    :PROPERTIES:
    :ID: 432400
    :END:
    - [ ] Implement Org Property Drawer to Notion Database Property mapping.
    - [ ] Support recursive child export (preserving hierarchy).
    - [ ] Handle Org Status mapping to Notion 'Status' or 'Checkbox' properties.
*** TODO Phase 4: Validation & Tooling
    :PROPERTIES:
    :ID: 79855549
    :END:
    - [ ] Create ~export_to_notion~ MCP tool.
    - [ ] Implement unit tests for the block converter.
    - [ ] Integration tests with a test Notion workspace.
** PROG Tool Design & AI UX Improvements [3/13] :ux:ui:design:
   :PROPERTIES:
   :ID: 30384709
   :END:
   - [x] Standardize tool returns: Ensure all ~manage_*~ tools return a CSV of affected items (currently ~manage_bullet~ and ~manage_text~ can return empty strings).
   - [x] Implement "Neighborhood" responses: Optionally return the parent and its immediate children after an add/move operation to verify hierarchy.
   - [ ] Improve tool documentation/schemas to explicitly suggest bulk operations for better token efficiency and state consistency.
   - [ ] Implement an ~output_format~ flag for ~query_items~ supporting CSV (default), Markdown, and Raw (Org) to allow switching between machine-efficient and human-readable views.
   - [ ] Quiet down CLI output for ~embed~ and other commands: use summaries or progress bars instead of full table dumps.
   - [ ] *Design Decision*: Refactor `manage_*` tools to use "Atomic Union" schemas (Method-based sub-schemas) instead of splitting tools. Preserves atomicity while increasing strictness.
   - [ ] *Design Decision*: Extract movement logic into a generic `move_item` tool that works for any `Render` node. Remove movement methods from `manage_bullet`.
*** DONE Response Metadata Enhancement [9/9]
    CLOSED: [2026-02-15 Sun 21:47]
    :PROPERTIES:
    :ID: 71585143
    :END:
    - [x] items_affected: count of items modified - quick count without parsing CSV
    - [x] execution_time_ms: log operation duration in milliseconds for performance debugging to the log file
    - [x] timestamp: RFC3339 timestamp for auditing and debugging
    - [x] Consider: backward compatibility - make metadata optional via output_format flag
    - [x] Scope: Which tools need metadata? (manage_header, manage_bullet, manage_text, query_items, all?)
    - [x] Format alternatives: (A) metadata object at array start, (B) wrapper around response, (C) new CSV fields
    - [x] Backward compatibility: Existing AI workflows must not break - consider output_format flag to toggle
    - [x] Prior art: Research other MCP servers for similar response metadata patterns
    - [x] v2 considerations: Response versioning, schema evolution, streaming metrics
*** TODO Enhanced Change Metadata in Tool Responses: Add old/new values for Progress and Status columns in CSV output
    :PROPERTIES:
    :ID: 27819683
    :END:
    - [ ] Include Progress column with old -> new values (e.g., "0/3" -> "2/3") showing what changed
    - [ ] Include Status column with old -> new values (e.g., "TODO" -> "PROG") showing status transitions
    - [ ] Include UID of the header/bullet that changed
    - [ ] Include Preview of both old and new values for context
    - [ ] Apply to manage_bullet responses (after complete/toggle/remove operations)
    - [ ] Apply to manage_header responses (after status changes)
    - [ ] Make it work with existing output_format flag (CSV, Markdown, Raw)
*** TODO Add a log access tool for debugging
    :PROPERTIES:
    :ID: 11489108
    :END:
    - [ ] Filter by log level (ERROR, WARN, INFO, DEBUG)
    - [ ] Filter by date/time range
    - [ ] Keyword search in log messages
    - [ ] Get recent N entries (tail -n style)
    - [ ] Filter by source/module
    - [ ] Error-only view (show only ERROR level)
    - [ ] Tail mode (follow logs in real-time)
*** TODO Quiet CLI output for embed and other commands :perf:ui:ux:cli:
    :PROPERTIES:
    :ID: 79657895
    :END:
    - [ ] *Source of Truth*: `OrgFileFromReader` in `orgmcp/file.go` has a hardcoded `fmt.Fprintf(os.Stderr, "%s", PrintTable(...))` call.
    - [ ] *Technical Constraint*: MCP server protocol expects a clean `stdout`. Currently, `os.Stderr` is being flooded with table dumps, making logs hard to read and potentially causing issues in some environments.
    - [ ] *Proposed Change*: Replace the full table dump with a one-line summary unless a `--verbose` flag is provided to the CLI.
*** TODO Issue-17 feat: Temporary UIDs for atomic multi-operation tool calls :issue:17:mcp:ux:memory::
    :PROPERTIES:
    :ID: 30379703
    :END:
    - [ ] *Gap*: Currently, adding an item in a `manage_*` call does not update the `OrgFile.items` map in-memory for that same call. This prevents subsequent operations (like adding a child to the newly created item) within the same tool execution.
    - [ ] *Proposal*: When an item is added, generate a temporary UID (e.g., `temp:rand8`) and register it in the `items` map immediately. This allows the tool to reference the new item for the remainder of the call.
    - [ ] *Decision Resolved: Temporary UIDs (AI-Specified)*: AI can specify a `temp_uid` (e.g., `temp:new-header`) in an `add` operation. This ID is registered in the in-memory `items` map immediately, allowing subsequent operations in the same call to reference it. Not persisted to disk.
    * *Technical Design: Temporary IDs (AI-Only)*: Implement `temp:[random]` prefix for in-memory-only addressing. UIDs are valid only for the duration of a single `tools/call` JSON-RPC execution.
    * *Implementation*: `OrgFile.RegisterTemp(Render) Uid` will be called during `AddChildren`. `GetUid` will resolve both stable and `temp:` prefixed IDs.
*** TODO Issue-18 feat: Implement oneOf/Discriminated Union schema generation for strict AI tool schemas :issue:18:schema:mcp:ai::
    :PROPERTIES:
    :ID: 40413358
    :END:
    [[https://github.com/P3rtang/org-mcp/issues/18]]
** DONE Tag Management & Discovery Improvements
   CLOSED: [2026-02-14 Sat 14:30]
   :PROPERTIES:
   :ID: 71742063
   :END:
   - [x] Implement a ~list_tags~ tool or a ~TAGS~ summary in ~status_overview~ to provide a global view of all used tags.
   - [x] Allow filtering ~query_items~ by multiple tags (AND/OR logic) to improve discovery.
   - [x] Add a "Tag Cloud" or frequency count to help AI models understand the "Hot Topics" of a project.
   * *Context*: Finding the AI session header was difficult because searching for specific tags (like ~:context:ai:session:~) isn't currently a first-class discovery mechanism.
* Project Terminology :memory:
  :PROPERTIES:
  :ID: 8251913
  :END:
  | Term                | Description                                                           | Avoid Using This Term       |
  |---------------------+-----------------------------------------------------------------------+-----------------------------|
  | *OrgFile*           | The top-level document structure representing the entire `.org` file. | File, document, tasks       |
  | *Header*            | A structural node starting with `*` asterisks.                        | Section, title, item        |
  | *Bullet*            | A list item starting with `-` or `*` within a header.                 | List, checkbox, sub-task    |
  | *PlainText*         | Indented text content that is not a header or bullet.                 | Body, note, text block      |
  | *Render*            | The interface implemented by all nodes (Headers, Bullets, PlainText). | Node, element, component    |
  | *UID*               | The unique identifier (e.g., `99998888` or `123.b0.t0`).              | ID, key, index              |
  | *Items Map*         | The `of.items` lookup table used for stable UID addressing.           | Cache, registry, dictionary |
  | *Stable Indexing*   | The principle that indices remain constant within a single tool call. | Order, position             |
  | *Tool Callback*     | The function executed by the MCP server for a specific tool.          | Action, handler             |
  | *Actionable Export* | The process of moving an Org task to a GitHub Issue.                  | Migration, sync             |
  | *Two-Way Linking*   | The requirement to cross-reference Org UIDs and GitHub Issue numbers. | Linking, reference          |
* Issues
  :PROPERTIES:
  :ID: 78690404
  :END:
** TODO Issue-10 fix: bullet-child parsing - Indented text incorrectly concatenated to bullet content :issue:10:bullet-parsing:
   :PROPERTIES:
   :ID: 81006049
   :END:
   * Description**: Indented plain text under a bullet is incorrectly concatenated to the bullet's content instead of being parsed as a separate ~PlainText~ child. This makes it impossible to target the text specifically for updates or removal without modifying the entire bullet.
   * Current Behavior**: Querying a bullet with indented text returns ~CHILDREN_COUNT: 0~ and the raw content contains both the bullet text and the indented text separated by a newline (\n).
   * Expected Behavior**: Indented text should be parsed as a separate ~PlainText~ node with a UID format of ~[bullet_uid].t0~.
   * Reproduction**: Run ~go test -v ./tools/test/... -run TestTextTool/UpdatePlainTextUnderBullet~. The test fails because ~99998891.b0.t0~ is not found in the ~items~ map.
   * Reproduction Branch**: ~repro/bullet-child-parsing~
   * Context**: Source UID ~99998891.b0.t0~ in ~tools/test/test.org~.
   [[https://github.com/P3rtang/org-mcp/issues/10]]
* PROG Issue-11 feat: Table Support :issue:11:table:parsing:memory:
  :PROPERTIES:
  :ID: 72407620
  :END:
  [[https://github.com/P3rtang/org-mcp/issues/11]]
** Discovery & Context Mapping
   :PROPERTIES:
   :ID: 53285059
   :END:
   * *Source of Truth*: Org-mode tables are delimited by `|` characters. Multiple rows form a contiguous table.
   * *Current Infrastructure*: `OrgFileFromReader` handles headers (`*`) and indented lines. Tables can be top-level or indented.
   * *Dependency Analysis*: Relies on the fixed `PlainText` vs `Bullet` parsing to ensure tables don't get swallowed into bullet content incorrectly.
   - [x] *Decision Resolved: Separator Handling*: Separators are metadata (list of divider indices). DataRows are addressable 0-N. Render logic generates `|---+---|` based on column widths.
   - [x] *Current Infrastructure Update*: `ParseIndentedLine` incorrectly treats `|` as `PlainText`. Integration point: Add `case '|'` to `ParseIndentedLine`.
   * *UID Stability Requirement*: Proposed `parent_uid.tbl[index]` to avoid collision with `.t` (PlainText) and `.b` (Bullet).
   - [ ] *Decision Branch: Separator Handling*: Determine if separators are addressable rows or metadata. (Current: Under Discussion)
   - [x] *Decision Resolved: Surgical Addressing*: Dedicated `read_table` tool for complex queries (row/col ranges). `manage_table` uses coordinate-based (R,C) addressing to keep queries surgical and token-efficient.
   - [x] *Decision Resolved: Alignment Strategy*: The `Render()` method will *always* auto-format with correct column widths. Correctness and human-readability are prioritized over minimal diff noise.
   - [x] *Decision Resolved: Parsing Fluidity*: `OrgFileFromReader` and `ParseIndentedLine` will both detect `|` as a structural marker. Tables are first-class `Render` nodes, capable of being children of `Header` or `Bullet`.
   - [ ] *Strategic Gap*: `OrgFileFromReader` panics on top-level text. High priority fix.
   - [ ] *Strategic Gap*: `items` map doesn't sync after `add` operations. Fix required for 'Temporary IDs'.
   - [ ] *Design Decision*: Support `#+NAME` as a stable UID for Tables.
   - [x] *Decision Resolved: Naming Stability*: `#+NAME` is the primary UID. If the user changes it, the AI must re-discover it via `query_items`. Not a system-level concern.
** Requirement Distillation
   :PROPERTIES:
   :ID: 17984267
   :END:
   * *Functional*: Reading/Writing Org tables via MCP tools. Support for CSV-style addressing (e.g., `row:1, col:2`).
   * *Technical*: Token optimization — don't return the full table if only one cell is needed. Efficient re-rendering preserving alignments.
   * *Gap Analysis*: Missing `Table` struct, table parser, and `manage_table` tool implementation.
   - [ ] *Critical Requirement*: `manage_table` must return a "Neighborhood" view (surrounding rows) rather than the full table to save tokens.
   - [ ] *Critical Requirement*: Table struct must handle "Ragged Rows" (rows with mismatched column counts) gracefully during `Render`.
   - [x] *Decision Resolved: Surgical Return*: `manage_table` returns a summary/neighborhood by default. Full table returned only if `show_table: true` is passed.
   - [x] *Decision Resolved: Structural Validation*: Rows with mismatched column counts will be normalized (padded/truncated) during `Render` with a warning returned to the user.
** Data & Resource Mapping
   :PROPERTIES:
   :ID: 26174735
   :END:
   * *Logic*: New `orgmcp/table.go` implementing `Render` interface. Update `ParseIndentedLine` to handle `|`.
   * *API*: New `manage_table` tool in `tools/table.go` with `read_cell`, `update_cell`, `add_row`, `delete_row` methods.
   * *Data*: `Table` struct with `rows [][]string` and `has_header bool` (if separator `|---|` exists).
   * *Block Parsing*: `NewTableFromReader` must peek at subsequent lines to swallow contiguous rows into a single node.
   * *Alignment Logic*: Implement a `Format()` method that re-calculates column widths after cell edits.
   * *Data Model*: `Table` struct will contain `Data [][]string` and `Dividers []int` (indices after which a separator line appears).
   * *Tool Interface*: `read_table` (UID, Rows[], Cols[]) -> JSON/CSV. `manage_table` (UID, Method, Row, Col, Content).
   * *Algorithm*: `Table.Render` will first scan all rows to determine `max_width` per column, then apply padding during string building. Divider lines will be generated to match the total table width.
   * *Parser Integration*: Update `OrgFileFromReader` and `ParseIndentedLine` to switch to `NewTableFromReader` upon encountering a `|`. Multi-line consumption handled within `NewTableFromReader`.
** Strategic Execution Plan
   :PROPERTIES:
   :ID: 68320744
   :END:
   * *Phase 1*: Foundation. Implement `Table` struct and basic line-by-line parsing in `orgmcp`.
   * *Phase 2*: Logic. Surgical cell editing and row manipulation logic. CSV export support.
   * *Phase 3*: Validation. Integration tests with complex table structures and alignment verification.
** Stakeholder Communication & Timeline
   :PROPERTIES:
   :ID: 80327750
   :END:
   * *Estimated Effort*: Medium. The table parsing and surgical cell editing require careful handling of indices and formatting.
   * *Critical Path*: Correctly parsing row delimiters and separator lines (`|---|`) without breaking surrounding text or bullet context.
   * *Milestone 1*: Table parsing and `read_table` tool (Phase 1 Foundation). Goal: End of this session.
   * *Milestone 2*: Surgical cell/row editing and automated alignment (Phase 2 Logic). Goal: Next session.
* DONE Issue-12 fix: Header corruption when updating text content :issue:12:parsing:text:memory::
  CLOSED: [2026-03-28 Sat 15:25]
  :PROPERTIES:
  :ID: 27277242
  :END:
  - [x] *Description*: Modifying text content under a header via `manage_text` (update) results in the subsequent header being merged into the text line, stripping the newline separator.
  - [x] *Current Behavior*: `Updated text.* Header` results in loss of structural integrity. Header is no longer a top-level node.
  - [x] *Reproduction*: `TestTextTool/HeaderCorruptionRepro` in `tools/test/text_tool_test.go` and `test.org`.
  - [x] *Fix*: Ensure `PlainText.SetContent` in `orgmcp/plain.go` appends a newline if it's missing, or ensure `manage_text` doesn't strip it. (SetContent should probably be the authoritative gatekeeper).
  [[https://github.com/P3rtang/org-mcp/issues/12]]
